[ ] whole `assets` folder added with haxe `--resource` 
[ ] variable interpolation in target output filenames `-D version="0.0.0" --jvm meow-$version.jar`
[ ] other minecraft version and/or modloader support (?)
* [ ] simplified config for multiloader n multiversion (xml? toml?)